# Discord Blacklist Bot

Advanced moderation bot with local/global blacklists, reasons, CSV/JSON import/export, confirmation buttons, and Docker support.

## Features
- Auto-ban on join
- Local & global blacklists
- Reasons for bans
- Import/export (CSV + JSON)
- Mod logging
- Docker ready

## Quick Start
```bash
docker compose up -d --build
